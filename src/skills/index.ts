/**
 * Re-export of canonical SkillRegistry implementation from AgentOS.
 * Kept as a barrel for backwards compatibility with consumers that
 * import from `wunderland/skills`.
 */

export * from '@framers/agentos/cognition/skills';
